<%- include("../partials/header", { pageTitle: "Yêu cầu bị từ chối" }) %>

<div class="text-center py-5">
  <p class="display-6 mb-2">403</p>
  <h1 class="h4">Yêu cầu bị từ chối</h1>
  <p class="text-secondary"><%= message %></p>
  <a href="<%= currentUser ? '/notes' : '/auth/login' %>" class="btn btn-primary mt-2">
    Quay lại
  </a>
</div>

<%- include("../partials/footer") %>
